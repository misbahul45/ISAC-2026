info:
  name: documents-show
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/registrations/me/documents"
  headers:
    - name: Accept
      value: application/json
    - name: ""
      value: ""
  auth:
    type: bearer
    token: "{{apiToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  #### GET /api/registrations/me/documents — RegistrationController@getDocuments
  - **Deskripsi**: Ambil data dokumen yang sudah di-upload (URL dokumen & twibbon).
  - **Middleware & Auth**: `auth:sanctum`. Sumber: `routes/api.php:69`.
  - **Request**: Header `Authorization: Bearer <token>`.
  - **Alur Proses**: `$team` langsung → `new DocumentsFormResource($team)`.
    `DocumentsFormResource` (`app/Http/Resources/DocumentsFormResource.php:15-21`):
    documentUrl, twibbonUrl.
  - **Database yang Terkena**: `teams` — SELECT.
  - **Efek Samping**: Tidak ada.
  - **Response** (200): `{status, message, data: DocumentsFormResource, metadata, error}`.
