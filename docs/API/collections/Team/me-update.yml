info:
  name: me-update
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/teams/me"
  headers:
    - name: Accept
      value: application/json
    - name: ""
      value: ""
  body:
    type: json
    data: |-
      {
          "name": "Alpha Team",
          "phone": "081234567890",
          "institution_name": "SMA Negeri 1 Surabaya",
          "institution_address": "{\"province\":\"Jawa Timur\",\"city\":\"Surabaya\",\"address\":\"Jl. Wijaya Kusuma No. 48\"}",
          "document_url": "https://drive.google.com/drive/folders/team-documents",
          "twibbon_url": "https://drive.google.com/drive/folders/team-twibbon"
      }
  auth:
    type: bearer
    token: "{{apiToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  #### PATCH /api/teams/me — TeamController@update
  - **Deskripsi**: Update parsial profil Team.
  - **Middleware & Auth**: `auth:sanctum`. Sumber: `routes/api.php:33`.
  - **Request** (validasi `UpdateTeamRequest`,
    `app/Http/Requests/Team/UpdateTeamRequest.php:17-26`):
    - `name`: sometimes, required, string, min:2, max:255
    - `phone`: sometimes, nullable, string, max:20
    - `institution_name`: sometimes, nullable, string, max:255
    - `institution_address`: sometimes, nullable JSON string alamat terstruktur
    - `document_url` dan `twibbon_url`: optional HTTPS Google Drive URL
  - **Alur Proses**: `teamService->updateProfile($user, validated)` →
    `teamRepository->update($team, $data)` (`app/Services/TeamService.php:19-23`,
    `app/Repositories/TeamRepository.php:13-18`, lalu `$team->fresh()`).
  - **Database yang Terkena**: `teams` — UPDATE.
  - **Efek Samping**: Tidak ada.
  - **Response** (200): `{status:'success', message:'Team profile updated
    successfully.', data: TeamResource, metadata:{}, error:null}`.
  - **Catatan**: `institution_address` divalidasi sebagai JSON string terstruktur.
