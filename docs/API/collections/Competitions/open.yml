info:
  name: open
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/competitions/open"
  headers:
    - name: Accept
      value: application/json
    - name: ""
      value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  #### GET /api/competitions/open — CompetitionController@open
  - **Deskripsi**: Daftar kompetisi yang sedang dibuka pendaftarannya (status = REGISTRATION_OPEN).
  - **Middleware & Auth**: Publik. Sumber: `routes/api.php:45`.
  - **Request**: Tidak ada parameter.
  - **Alur Proses** (`CompetitionService::getOpenCompetitions`):
    Filter `status = REGISTRATION_OPEN`. Return `CompetitionResource::collection`.
  - **Database yang Terkena**: `competitions` — SELECT (filter status).
  - **Efek Samping**: Tidak ada.
  - **Response** (200): `{status, message, data: [CompetitionResource], metadata, error}`.
