info:
  name: me
  type: http
  seq: 5

http:
  method: GET
  url: http://127.0.0.1:8000/api/auth/me
  headers:
    - name: Accept
      value: application/json
    - name: ""
      value: ""
  auth:
    type: bearer
    token: 1|hOixlBJ3oZdkkHn1HLArB15GGwkHS36SzZuXSpD7b41814ee

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  #### GET /api/auth/me — AuthController@me
  - **Deskripsi**: Data team yang sedang login.
  - **Middleware & Auth**: `auth:sanctum`. Sumber: `routes/api.php:54`.
  - **Request**: Header `Authorization: Bearer <token>`.
  - **Alur Proses**: `$team = $request->user()` → `TeamAuthResource($team)`
    (`app/Http/Resources/TeamAuthResource.php`).
  - **Database**: `teams` SELECT (Auth::user()).
  - **Response** (200): `TeamAuthResource` → id, code, email, name, phone,
    schoolName, status (uppercase).
  - **Catatan**: Tidak ada.

