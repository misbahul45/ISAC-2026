info:
  name: logout
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/auth/logout"
  headers:
    - name: Accept
      value: application/json
    - name: ""
      value: ""
  auth:
    type: bearer
    token: "{{apiToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  #### POST /api/auth/logout — AuthController@logout
  - **Deskripsi**: Revoke Sanctum token yang sedang dipakai.
  - **Middleware & Auth**: `auth:sanctum` (throttle:10,1). Sumber: `routes/api.php:87`.
  - **Request**: Header `Authorization: Bearer <token>`.
  - **Alur Proses** (`AuthService::logout`, `app/Services/AuthService.php:70-73`):
    `$team->currentAccessToken()?->delete()` (hapus hanya token request ini).
  - **Database**: `personal_access_tokens` DELETE (1 baris).
  - **Response** (200): data null.
  - **Catatan**: Tidak ada.

